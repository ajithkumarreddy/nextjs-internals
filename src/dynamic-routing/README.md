### Dynamic Routes

1. Dynamic route segments /blog/[slug]/page.tsx
2. Dynamic route [id]
3. Multiple dynamic route segments
4. client params - useParams() - next/navigation
5. params can be accessed in page.tsx/layout.tsx/generateMetaData() function/route.ts server APIs
